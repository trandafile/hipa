# Esempio di file .env per la configurazione del progetto Streamlit
# Crea un file chiamato `secrets.toml` dentro la cartella `.streamlit` oppure usa le impostazioni segrete di Streamlit Cloud.
# Per ambiente locale, puoi anche usare un file `.env` e python-dotenv.

# 1. Configurazione Google OAuth 2.0 (Login Utente)
GOOGLE_CLIENT_ID="il_tuo_client_id_oauth.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="il_tuo_client_secret"
GOOGLE_REDIRECT_URI="http://localhost:8501" # Cambia in produzione con l'url dell'app

# 2. Configurazione Google Service Account (Accesso Backend a Drive e Sheets)
# Puoi fornire il percorso completo del file JSON SCARICATO da Google Cloud...
GOOGLE_APPLICATION_CREDENTIALS="percorso/al/tuo/service_account.json"

# ... OPPURE puoi incollare il contenuto JSON direttamente qui (meglio per Streamlit Cloud Secrets)
GOOGLE_SERVICE_ACCOUNT_JSON='{"type": "service_account", "project_id": "...", "private_key": "...", "client_email": "..."}'

# 3. IDs dei Fogli Google e Cartelle Drive
GOOGLE_SHEET_ID="ID_DEL_FOGLIO_GOOGLE_(estraibile_dall_url)"
GOOGLE_DRIVE_FOLDER_ID="ID_DELLA_CARTELLA_DRIVE_PER_I_PDF_TEST"
